// src/schema/index.ts
import dealSchema from "./dealSchema";
import customerSchema from "./customerSchema";
import taskSchema from "./taskSchema";

const schema = [dealSchema, customerSchema, taskSchema];

export default schema;
